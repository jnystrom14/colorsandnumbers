<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Print Calendar</title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <style>
      /* Optional: force landscape in older browsers */
      @page { size: landscape; margin: 0; }
      body { margin: 0; padding: 1em; }
    </style>
  </head>
  <body onload="window.print()">
    <%= yield %>
  </body>
</html>
